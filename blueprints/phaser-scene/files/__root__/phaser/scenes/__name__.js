import Phaser from 'phaser';

export class <%= classifiedModuleName %> extends Phaser.Scene {

  constructor() {
    super({key: '<%= dasherizedModuleName %>'});
  }

  // load game assets such as images and sounds
  preload () {
  }

  // create game scene
  create() {
  }

  // main game loop for this scene
  update() {
  }
}
